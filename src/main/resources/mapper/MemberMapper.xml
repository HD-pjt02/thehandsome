<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.thehandsome.app.mapper.MemberMapper">
	<select id="isdupluid" parameterType="String" resultType="Long">
		SELECT 1
		FROM MEMBER
		WHERE id = #{uid}
	</select>
	
	<insert id="joinMember" parameterType="memberDTO">
		INSERT INTO MEMBER
		(MNO,ID,PASSWORD,EMAIL,NAME,BIRTH,MILEAGE,REGDATE,LASTLOGINDATE,AMOUNT)
		VALUES 
		(MEMBER_SEQ.NEXTVAL,#{id},#{password},#{email},#{name},#{birth},0,SYSDATE,SYSDATE,0)
	</insert>
	
	<select id="memberLogin" parameterType="memberDTO" resultType="memberDTO">
		SELECT *
		FROM MEMBER
		WHERE id = #{id}
	</select>
	
</mapper>